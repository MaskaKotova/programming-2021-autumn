#include <iostream>
using namespace std;


int main()
{
   int m = 0;
   
   cin » m;
   
   if (((m % 4 == 0) && (m != 0)) || (m == 1))
   {
      cout « "YES";
   }
   else
   {
      cout « "NO";
   }
   
   return EXIT_SUCCESS;
}

#include <iostream>

using namespace std;

int main (int argc, char* argv[])
{
   int a = 0;
   int b = 0;
   int e = 0;

   cin » a;
   cin » b;
   cin » e;

   if (n <= k)
   {
      cout << m * 2;
   }
   else if ((n * 2) % k == 0)
   {
      cout << (((n * 2) / k) *m);
   }
   else
   {
      cout << ((((n *2) / k) *m) + m);
   }
   return EXIT_SUCCESS;
}
